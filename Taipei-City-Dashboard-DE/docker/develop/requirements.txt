# --- Airflow 3.x core + providers ---
# 版本由 Dockerfile 的 --constraint 自動解析，不手動 pin。
apache-airflow-task-sdk
apache-airflow-providers-fab
apache-airflow-providers-postgres
apache-airflow-providers-celery

# --- Data / GIS utilities ---
# 不 pin 版本，由 constraints file 管有交集的套件；
# 沒在 constraints 裡的套件 pip 會裝最新相容版。
geopandas
fiona
pyogrio
GeoAlchemy2
pytest
openpyxl
wget
Rtree
psycopg2-binary
geopy
XlsxWriter
pyminizip
minio
odfpy
